%h2= trip.name

- if trip.begun_on.present?
  %h3
    = trip.begun_on
    to
    = trip.ended_on

%h3 Places visited

%ul
  - trip.places.each do |place|
    %li
      = place.name

= gmaps(markers:   {data: geocoded(trip).to_json},
        polylines: {data: polylines(trip).to_json})

-#This should go at the end of the application layout for best page loading experience
= yield :scripts
